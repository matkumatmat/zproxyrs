//! Library root so `tests/` can import the bridge without duplication.
//! Rule #4: unit tests live outside `src/` under `tests/`.

pub mod config;
pub mod constants;
pub mod docs;
pub mod enums;
pub mod printer;
pub mod routes;
pub mod status;
